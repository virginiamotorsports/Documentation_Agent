# TODO
1. Create webhook so that Box re-runs ingestion async every time Engineering folder is updated 
    - download files 
    - embedded into vectors
    - store/add to knowledge base 
2. Create knowledge base for Formula26 Engineering folder 
3. Create agent 
4. Create slack bot 

# Code Maintanence 
If you want to add a package dependency run `poetry add <package_name>`

Run `uvx ruff format` before commiting any new code 